[services]
  [services.ml]
  root = "ml_service"
  start = "uvicorn main:app --host 0.0.0.0 --port $PORT"
  healthcheckPath = "/health"
  autoDeploy = true

[variables]
PORT = "8080"
PYTHONUNBUFFERED = "1"

